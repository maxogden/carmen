
package carmen.proto;

option optimize_for = LITE_RUNTIME;

message object {

    message item {
        required int64 key = 1;
        repeated int64 val = 2 [ packed = true ];
    }

    required int64 item_count = 1;
    repeated item items = 2;
}
